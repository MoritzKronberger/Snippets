{ "name": "postgres_db",
  "version": "1.0.0",
  "author": "Moritz Kronberger",
  "license": "MIT",
  "description": "Postgres: Database for Web-Prog Group Project",
  "scripts": {
    "serve": "docker compose up",
    "stop": "docker compose down",
    "reset": "docker compose down && docker stop `docker ps -a -q` && docker rm `docker ps -a -q`; docker volume prune && docker compose up --build"
  },
  "repository": {
      "type": "git",
      "url": "https://gitlab.multimedia.hs-augsburg.de/mokro/gruppenprojekt-webprogrammierung"
  },
  "keywords": ["Postgres",
               "JSON",
               "REST"
              ]
}
  